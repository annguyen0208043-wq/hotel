import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const Rooms = () => {
  return (
    <Card>
      <Title level={3}>Quản lý phòng</Title>
      <p>Trang quản lý phòng đang được phát triển...</p>
    </Card>
  );
};

export default Rooms;