import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const RoomTypes = () => {
  return (
    <Card>
      <Title level={3}>Quản lý loại phòng</Title>
      <p>Trang quản lý loại phòng đang được phát triển...</p>
    </Card>
  );
};

export default RoomTypes;